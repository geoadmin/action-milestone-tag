---
# WARNING this file is managed by terraform and cannot be edited manually, see
# tf/github/geoadmin/modules/github-workflows-infra/main.tf
name: on-pr

on:
  pull_request:
    types:
      - opened
      - reopened
      - synchronize
      - edited
      - labeled
      - unlabeled

jobs:
  infra:
    uses: geoadmin/.github/.github/workflows/pr-auto-infra.yml@master
